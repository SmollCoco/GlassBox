# glassbox-benchmark Detailed UML

```mermaid
classDiagram
    class BenchmarkResult {
        +case
        +unit
        +notes
        +group
        +library
        +metric
        +value
    }
```
